# Configuration Docker
USER_ID=1000
GROUP_ID=1000

# Configuration Symfony
APP_ENV=dev
APP_SECRET=change_me_in_production

# Base de données
DATABASE_URL="mysql://elliteam:elliteam@mysql:3306/elliteam?serverVersion=8.0"

# Pour PostgreSQL (alternative)
# DATABASE_URL=postgresql://symfony:symfony@postgres:5432/symfony?serverVersion=15&charset=utf8

# Mailer (MailDev en dev)
MAILER_DSN=smtp://maildev:1025

# Redis (si utilisé)
REDIS_URL=redis://redis:6379

# Messenger (si utilisé)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
